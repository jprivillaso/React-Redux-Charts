import React from 'react'

export default React.createClass({
  render() {
    return (
      <h5>Navigate trough the tabs to see the different visualization
    of the wallmart stores' sellings</h5>
    );
  }
})
